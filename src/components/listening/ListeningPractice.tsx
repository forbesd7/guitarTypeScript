import * as React from "react";
//import soundfile from "./sound2.mp3";
//import Sound from "react-sound";
export interface ListeningPracticeProps {}

const ListeningPractice: React.SFC<ListeningPracticeProps> = () => {
  return <div>practice time bitch</div>;
};

export default ListeningPractice;

// <Sound volume={100} url={soundfile} playStatus={Sound.status.PLAYING} />
